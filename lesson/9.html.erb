<%# `=~`を使うと、正規表現がマッチした場合は文字列中のマッチした位置（０以上の数値）が返り、マッチしなかった場合はnilが返る。 %>


<%# マッチした場合はマッチした文字列の開始位置が返る（つまり真） %>
'123-4567' =~ /\d{3}-\d{4}/
<%# #=> 0 %>

<%# # マッチしない場合はnilが返る（つまり偽） %>
'hello' =~ /\d{3}-\d{4}/
<%# #=> nil %>


<%# # if文で=~を使うとマッチしたかどうかを判別できる %>
if '123-4567' =~ /\d{3}-\d{4}/
  puts 'マッチしました'
else
  puts 'マッチしませんでした'
end
<%# #=> マッチしました %>